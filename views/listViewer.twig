<fieldset>
  <legend>{{ legend }}</legend>
  {% if list is iterable and list | length > 0 %}
    <ul>
      {% for item in list %}
        <li>{{ item | raw }}</li>
      {% endfor %}
    </ul>
  {% endif %}
</fieldset>